# Payment Instruction Parser API

A **production-ready**, **type-safe**, **well-tested** REST API that parses and executes financial transaction instructions using **string manipulation only** (no regex).

---

## Features

- **Zero Regex** – Full parsing using `.split()`, `.indexOf()`, `.substring()`
- **Case-insensitive keywords & currencies**
- **Supports DEBIT & CREDIT instruction formats**
- **Future-dated transactions** (`ON YYYY-MM-DD`)
- **UTC date comparison**
- **Comprehensive validation** with **exact status codes**
- **Zod schema validation**
- **Structured logging & error handling**
- **100% TypeScript**

---

## Endpoint
